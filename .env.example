# Copy to .env and set values in production
SECRET_KEY=change-this-to-a-secure-random-value
# For Render, set DATABASE_URL in the service environment.
# Local default uses sqlite in instance/ folder.
# The app creates the instance/ folder automatically if needed.
# Example sqlite URL: sqlite:///instance/story_posts.db
DATABASE_URL=